# WRAITH v2.0 — Dependencies
# Core
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0

# AI Providers
openai>=1.0.0
anthropic>=0.18.0

# Web UI
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0

# Auth & Storage (optional — falls back to local mode)
supabase>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Code Protection (optional — for pro build)
# pyarmor>=8.0  # Uncomment for pro build

# Reports (optional — for PDF)
# weasyprint>=60.0  # Uncomment for PDF reports
